@model IEnumerable<Heat.Models.DanceType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DanceTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DanceTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DanceTypeID })
        </td>
    </tr>
}

</table>
