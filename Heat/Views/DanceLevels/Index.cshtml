@model IEnumerable<Heat.Models.DanceLevel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DanceLevel1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DanceLevel1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DanceLevelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DanceLevelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DanceLevelID })
        </td>
    </tr>
}

</table>
